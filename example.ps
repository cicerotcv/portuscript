seja x, y, z;

x = 3;
y = Numero(interface.entrada("Digite um número"));

z = x + y;

se (z < 10) {
  interface.saida("Número muito pequeno");
} senao {
  interface.saida("Número grande o suficiente")
}
